package com.talent360.ui;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class Talent360UiApplication {

    public static void main(String[] args) {
        SpringApplication.run(Talent360UiApplication.class, args);
        // Une fois lance, ouvrir http://localhost:8080 dans le navigateur
    }
}
